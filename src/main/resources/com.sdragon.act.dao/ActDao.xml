<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sdragon.act.dao.ActDao">
    <select id="listAct" resultType="act">
        SELECT roleid, treeid, parentid, rolename, text
        FROM work.rolepermissions;
    </select>
    <!--<select id="getById" parameterType="long" resultType="user">-->
        <!--select id,login_name loginName,user_name userName,password,telephone,email,is_enable enable,is_block block ,is_admin admin,birthday from work.t_user-->
        <!--WHERE id = #{id}-->
    <!--</select>-->
</mapper>
